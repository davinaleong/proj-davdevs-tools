---
import { getEntry } from 'astro:content'
import ToolLayout from './../../layouts/ToolLayout.astro'
import PasswordCreator from '../../components/react/PasswordCreator'

const entry = await getEntry('tools', 'qr-code-generator')

if (!entry) {
  throw new Error('Could not find entry')
}

const { data } = entry;
const { title, description, date } = data
---

<ToolLayout title={title} description={description} date={date}>
  <h2 class="text-3xl font-bold">🔑 Creative Password Generator</h2>

  <div class="mt-4">
    <PasswordCreator client:load />
  </div>

  <p class="mt-2">
    Generate memorable yet strong passwords by mixing phrases, foreign words, symbols, and digits — a perfect blend of creativity and security.
  </p>

  <ul class="list-disc list-inside mt-4 space-y-1">
    <li>Start with a long, meaningful phrase</li>
    <li>Randomly capitalize 2–4 words for variety</li>
    <li>Swap one keyword with its foreign-language translation</li>
    <li>Spell the translated word using the English alphabet</li>
    <li>Append a strong symbol (e.g. <code>@</code>, <code>#</code>, <code>$</code>, etc.)</li>
    <li>Finish with a 2–3 digit number</li>
    <li>See how long it would take to crack your password</li>
  </ul>

  <p class="mt-4">
    This approach creates passwords that are easier to remember but still hard to crack. Perfect for securing personal accounts, projects, and more.
  </p>
</ToolLayout>

